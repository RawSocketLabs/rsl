# rsl-compression

Accuracy-first compression and decompression contracts for the RSL stack. The scaffold keeps
streaming state, flush behavior, and reset boundaries explicit so future SSH compression and
other protocol consumers do not need ad-hoc wrappers.

No compression algorithm is implemented yet.
