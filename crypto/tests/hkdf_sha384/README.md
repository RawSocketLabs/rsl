# HKDF-SHA-384 public tests

Mirror of `tests/hkdf_sha256/` with `HashLen = 48`: Wycheproof cases (`known_answers.rs`, `wycheproof_fixtures.rs`), the 12,240-byte bound and stage composition (`boundaries.rs`), and the `hkdf` oracle (`differential.rs`). Provenance: `../vectors/hkdf-sha384/README.md`.
