# HMAC-SHA-384 public tests

Mirror of `tests/hmac_sha256/` with the SHA-384 parameters: RFC 4231 cases 1–7 (`known_answers.rs`), fragmentation (`streaming.rs`), uniform verification (`verification.rs`), and the `hmac` oracle (`differential.rs`). Provenance: `../vectors/hmac-sha384/README.md`.
