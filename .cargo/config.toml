# Workspace cargo config. Kept minimal on purpose.
[alias]
# Use the same workspace-root policy as CI. Run `cargo deny-check` (optionally
# selecting a check, e.g. `cargo deny-check bans`).
# Named `deny-check` rather than `deny` to avoid shadowing the cargo-deny subcommand.
deny-check = "deny --config deny.toml check"
