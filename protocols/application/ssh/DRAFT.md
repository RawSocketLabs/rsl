# DRAFT — ssh (unreviewed, WIP)

Migrated verbatim (with history) from `asyio-tools/protocols`
(application/ssh, source faa9250). Early/partial work — wire-format
modelling only, no working implementation yet.

**Status: draft.** Excluded from the workspace (`Cargo.toml` `exclude`);
does not build or ship. Requires significant review and completion before
promotion to a workspace member.
