# DRAFT — smb (unreviewed)

Migrated verbatim (with history) from `asyio-tools/protocols`
(application/smb, source faa9250) before that org is retired.

**Status: draft.** Excluded from the workspace (`Cargo.toml` `exclude`),
so it neither builds nor ships with the monorepo. Preserved on the
`draft/protocols-smb` branch; requires significant review — API, `unsafe`
audit, tests, and workspace/dependency wiring — before it is promoted to a
workspace member. Do not depend on it.
