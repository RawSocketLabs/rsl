schema = 1
required_string_fields = ["base", "installation_mode", "purpose", "consumers", "lifecycle", "profile_rationale"]
required_array_fields = ["capabilities", "enabled_skills", "local_instruction_paths", "unresolved"]
required_object_fields = ["components", "decisions", "validation"]
approval_gate = "approved must be true before rendering"
