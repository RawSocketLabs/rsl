# Task

The benchmark identifies `submit_samples` as an allocation hotspot. Change this
small Rust library to remove unnecessary work while keeping ownership clear.
Preserve the current error behavior and explain the verification you perform.
