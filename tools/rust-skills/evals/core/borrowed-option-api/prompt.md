# Task

Review this small public Rust library and improve the `render_label` API if its
ownership or borrowing contract is more restrictive than the behavior requires.
Preserve output behavior, update callers and tests, and report the validation
you actually perform.
