# Task

This internal billing library records settlements and renders statement lines.
Review how `Settlement` represents its progress and its amount, and improve the
type where the current representation admits values the domain does not.
Preserve the rendered statement lines and the settled total, update callers and
tests, and report the validation you actually perform.
