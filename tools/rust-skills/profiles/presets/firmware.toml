schema = 1
id = "firmware"
kind = "preset"
summary = "Application defaults for embedded, no-std, real-time firmware."
base = "application"
capabilities = ["embedded", "no-std", "real-time", "safety-critical"]
skills = []
implies = []
